# Pre-trained ML model storage directory
# Models are generated by: python -m ai_engine.ml.train
# Output files:
#   gradient_boosting.json  — Supervised fraud classifier (100 trees)
#   isolation_forest.json   — Unsupervised anomaly detector (150 trees)
#   training_report.json    — Training metrics and evaluation results
